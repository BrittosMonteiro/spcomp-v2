import DialogDefault from "../../../../../../components/Dialog/DialogDefault";

export default function DialogSupplier({ open, onClose, reload, supplier }) {
  return <DialogDefault></DialogDefault>;
}
